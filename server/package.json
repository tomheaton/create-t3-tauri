{
  "name": "server",
  "version": "0.1.0",
  "description": "",
  "main": "./src/index.ts",
  "bin": "./build.cjs",
  "scripts": {
    "dev": "ts-node .",
    "build": "tsc",
    "bundle": "esbuild ./dist --bundle --outfile=build.cjs --format=cjs --platform=node"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@fastify/cors": "^8.3.0",
    "@trpc/server": "^10.38.1",
    "fastify": "^4.22.2",
    "zod": "^3.22.2"
  },
  "devDependencies": {
    "esbuild": "^0.19.2",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
